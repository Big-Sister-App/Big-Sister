name: Lint

on: [pull_request]

jobs:
  eslint:
    name: Lint JS Files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: stefanoeb/eslint-action@1.0.2
        with:
          files: website/static/javascript